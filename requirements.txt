# Core dependencies for Tavily agent
tavily-python>=0.3.0
python-dotenv>=1.0.0

# Standard library imports (no installation needed):
# - os (built-in)
# - getpass (built-in)

# Jupyter notebook support (optional, for running the .ipynb file)
jupyter>=1.0.0
ipykernel>=6.0.0

langchain-openai 
langchain 
langchain-tavily 
langgraph